import { NavbarcolourDirectiveDirective } from './navbarcolour-directive.directive';

describe('NavbarcolourDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarcolourDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
