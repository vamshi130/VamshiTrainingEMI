import { BooksFilterPipePipe } from './books-filter-pipe.pipe';

describe('BooksFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BooksFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
